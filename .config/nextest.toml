[profile.default]
fail-fast = false
slow-timeout = { period = "30s", terminate-after = 2 }

[profile.ci]
fail-fast = false
slow-timeout = { period = "30s", terminate-after = 2 }
junit.path = "target/nextest/ci/junit.xml"
